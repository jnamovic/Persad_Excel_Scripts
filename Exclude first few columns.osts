{"version":"0.2.0","body":"function main(workbook: ExcelScript.Workbook): {} {\n  // get first table since we only have one here\n  let table = workbook.getWorksheet('Sheet1').getTables()[0];\n  // get data\n  let rows = table.getRangeBetweenHeaderAndTotal().getValues();\n  let listOfRows = [];\n  for (let row of rows) {\n    let temp = row\n    let removed = temp.splice(2,8)\n    listOfRows.push(temp)\n  }\n  let res = Object.assign({}, listOfRows);\n  return res;\n}","description":"Exclude first few columns of unnecessary information. Extra the entire row as a list.","parameterInfo":"{\"originalParameterOrder\":[],\"parameterSchema\":{\"type\":\"object\",\"default\":{},\"x-ms-visibility\":\"internal\"},\"returnSchema\":{\"type\":\"object\",\"properties\":{}}}","apiInfo":"{\"variant\":\"synchronous\",\"variantVersion\":1}"}